config BR2_PACKAGE_NANO_CLOCK
	bool "nano-clock"
	depends on BR2_PACKAGE_LIBGTK3
	depends on BR2_PACKAGE_WAYLAND
	select BR2_PACKAGE_LIBGLIB2
	help
	  NanoOS clock application.
	  Features clock, stopwatch, and timer.
